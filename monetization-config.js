window.FOCUSEA_MONETIZATION = Object.freeze({
  enabled: false,
  publisherId: "",
  certifiedCmpReady: false,
  autoAds: true,
  placements: Object.freeze({
    news: Object.freeze({ slot: "", format: "auto", responsive: true }),
    academy: Object.freeze({ slot: "", format: "auto", responsive: true }),
    ports: Object.freeze({ slot: "", format: "auto", responsive: true })
  })
});
